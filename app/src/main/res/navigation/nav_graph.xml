<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/splashFragment">

    <fragment
        android:id="@+id/splashFragment"
        android:name="dev.anticlub.videoplayer.presentation.auth.SplashFragment"
        android:label="Splash">
        <action
            android:id="@+id/action_splash_to_login"
            app:destination="@id/loginFragment"
            app:popUpTo="@id/splashFragment"
            app:popUpToInclusive="true" />
        <action
            android:id="@+id/action_splash_to_playlist"
            app:destination="@id/playlistFragment"
            app:popUpTo="@id/splashFragment"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/loginFragment"
        android:name="dev.anticlub.videoplayer.presentation.auth.LoginFragment"
        android:label="Login">
        <action
            android:id="@+id/action_login_to_register"
            app:destination="@id/registerFragment" />
        <action
            android:id="@+id/action_login_to_playlist"
            app:destination="@id/playlistFragment"
            app:popUpTo="@id/loginFragment"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/registerFragment"
        android:name="dev.anticlub.videoplayer.presentation.auth.RegisterFragment"
        android:label="Register">
        <action
            android:id="@+id/action_register_to_login"
            app:destination="@id/loginFragment"
            app:popUpTo="@id/loginFragment"
            app:popUpToInclusive="true" />
        <action
            android:id="@+id/action_register_to_playlist"
            app:destination="@id/playlistFragment"
            app:popUpTo="@id/registerFragment"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/playlistFragment"
        android:name="dev.anticlub.videoplayer.presentation.playlist.PlaylistFragment"
        android:label="Playlists">
        <action
            android:id="@+id/action_playlist_to_channel"
            app:destination="@id/channelFragment" />
        <action
            android:id="@+id/action_playlist_to_player"
            app:destination="@+id/playerFragment" />
        <action
            android:id="@+id/action_playlist_to_login"
            app:destination="@id/loginFragment"
            app:popUpTo="@id/nav_graph"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/channelFragment"
        android:name="dev.anticlub.videoplayer.presentation.channel.ChannelFragment"
        android:label="Channels">
        <argument
            android:name="playlistId"
            app:argType="long"
            android:defaultValue="-1L" />
        <action
            android:id="@+id/action_channel_to_player"
            app:destination="@id/playerFragment" />
    </fragment>

    <fragment
        android:id="@+id/playerFragment"
        android:name="dev.anticlub.videoplayer.presentation.player.PlayerFragment"
        android:label="Player">
        <argument
            android:name="streamUrl"
            app:argType="string"
            android:defaultValue="" />
        <argument
            android:name="streamName"
            app:argType="string"
            android:defaultValue="" />
        <argument
            android:name="streamLogo"
            app:argType="string"
            android:defaultValue="" />
        <argument
            android:name="streamPlaylistName"
            app:argType="string"
            android:defaultValue="" />
        <argument
            android:name="licenseUrl"
            app:argType="string"
            android:defaultValue="@null"
            app:nullable="true" />
    </fragment>

</navigation>